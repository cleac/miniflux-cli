containers:
  flake8:
    setup:
      - !Alpine v3.8
      - !PipConfig { dependencies: true }
      - !Py3Install
        - flake8==3.5.0

commands:
  flake8: !Command
    container: flake8
    accepts_arguments: true
    description: Run flake8
    run: [flake8]
